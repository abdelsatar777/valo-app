import 'package:flutter/material.dart';

class CustomOptionsDataPage extends StatelessWidget {
  final Widget body;

  const CustomOptionsDataPage({super.key, required this.body});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: body);
  }
}
